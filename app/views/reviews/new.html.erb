<h2>Write a review for <%= @restaurant.name %></h2>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, in: 0..5, step: 1 %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
